export enum calendarOptions {
  Month = 'Month',
  Week = 'Week',
  Day = 'Day',
}

export enum helpfulStuff {
  Sunday = 1,
  Monday = 2,
  Tuesday = 3,
  Wednesday = 4,
  Thursday = 5,
  Friday = 6,
  Saturday = 7,
}

export enum RepeatFrequency {
  Never,
  Daily,
  Weekly,
  Monthly,
  Quarterly,
  Yearly,
}

export enum Importance {
  Low,
  Normal,
  High,
  Critical,
}
export enum CalendarView {
  Day,
  Week,
  Month,
}
